version: '2.1'
services:
    changedetection:
      image: cristacheda/filelist-api-whitelist:latest
      container_name: filelist-api-whitelist
      environment:
        - FL_USERNAME=your_username
        - FL_PASSWORD=your_password
        - CHECK_INTERVAL=10 #anything > 5 minutes
      restart: unless-stopped